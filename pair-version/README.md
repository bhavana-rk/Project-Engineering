# Pair Version

This folder contains the task manager app built using an AI pair programming assistant.

**Tool used:** GitHub Copilot
**Files:** 3 (`index.html`, `styles.css`, `app.js`)
**Live URL:** Pending hosting credentials

## Notes

The build is dependency-free and split into HTML, CSS, and JavaScript. The code keeps
filter matching, task rendering, and count calculation in separate named functions.
Browser verification covered adding, completing, and filtering tasks.
